# Tasks

- [x] Implement centered split-screen brochure modal for active hotel details in `src/App.jsx`.
- [x] Implement vanilla React mouse-move 3D tilt effect handler on hotel card items in `src/App.jsx`.
- [x] Rewrite CSS classes for `.hotel-detail-modal` and card 3D transitions in `src/App.css`.
- [x] Create React `<AnimatedCounter>` with IntersectionObserver triggers.
- [x] Replace static highlights numbers with `<AnimatedCounter>` tickers.
- [x] Implement scroll-driven vertical timeline tracker progress bar overlay in `src/App.jsx`.
- [x] Synchronize step dot and card active states directly to timelineProgress height to fix trigger lag mismatch.
- [x] Reduce global section-padding to 80px (desktop) / 50px (mobile) in index.css.
- [x] Reduce margins on headers, tabs, and timelines in App.css to tighten vertical spacing.
- [x] Verify build compiles cleanly via `npm run build`.
